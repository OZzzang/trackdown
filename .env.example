# Copy to server/.env and fill in. Never commit the real .env.

# AudD audio fingerprinting API token — https://audd.io
AUDD_TOKEN=

# MongoDB Atlas connection string (Phase 3+)
MONGODB_URI=

# Express port
PORT=3000
